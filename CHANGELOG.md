# Changelog

## Unreleased

- README: fixed npm import paths (`import "@thinking.tools/teff"`, not `import "teff"`).
- `package.json`: added an `exports` map and `types` entry.
- New: `REFERENCE.md` (complete single-file API reference), `llms.txt` (LLM/agent index), `teff.d.ts` (TypeScript declarations), `custom-elements.json` (custom elements manifest) — all shipped in the npm package.

## 1.0.2 — 2026-06-11

Current release on npm as `@thinking.tools/teff`. (1.0.0 was an empty shell — do not use.)

Earlier history predates this changelog; see git log.
